<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Company</title>
</head>
<body>
    <h1>Edit Company</h1>
    <form action="/companies/<%= company._id %>?_method=PUT" method="POST">
        <div>
            <label for="name">Company name</label>
            <input type="text" id="name" name="company[name]" value="<%= company.name %>" required>
        </div>
        <button>Update Company</button>
    </form>
    <a href="/companies/<%= company._id %>">Back to Companies</a>
</body>
</html>