<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Ad</title>
</head>
<body>
    <h1>Edit Ad</h1>
    <form action="/ads/<%= ad._id %>?_method=PUT" method="POST" enctype="multipart/form-data">
        <div>
            <label for="company">Company name</label>
            <input type="text" id="company" name="ad[company]" value="<%= ad.company %>" required>
        </div>
        <div>
            <label for="text">text</label>
            <textarea type="text" id="text" name="ad[text]" value="<%= ad.text %>" required><%= ad.text %></textarea>
        </div>
        <div>
            <label for="image">Image</label>
            <input type="file" id="image" name="image" value="<%= ad.image %>" multiple>
        </div>
        <button>Update AD</button>
    </form>
    <a href="/ads"><button>Back to Ads</button></a>
    <a href="/"><button>Home Page</button></a>
</body>
</html>