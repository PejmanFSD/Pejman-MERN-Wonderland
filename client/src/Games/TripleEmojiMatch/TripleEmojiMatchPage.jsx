import TripleEmojiMatch from './TripleEmojiMatch';

export default function TripleEmojiMatchPage({updateTotalPoint, currentUser}) {
    return (
        <TripleEmojiMatch updateTotalPoint={updateTotalPoint} currentUser={currentUser} />
    )
}